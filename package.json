{
  "name": "ops-copilot",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "test": "node --test",
    "check": "npm run build && npm test"
  }
}
